# 지침

## 링크/출처 확인 규칙 (2026-09-10 등록)

사용자가 특정 URL을 주고 "들어가서 확인하고 답변해"라고 명시적으로 요청하면:

1. **반드시 해당 URL 자체를 fetch해서 실제 내용을 읽는다.** 관련 키워드로 웹서치한
   결과나 다른 매체의 보도를 그 링크의 내용인 것처럼 답변해서는 안 된다.
2. fetch가 실패하거나(네트워크 정책 차단, 인증 필요, 404 등) 접근이 막히면,
   **답변 앞부분에 "이 링크는 확인하지 못했다"는 사실과 그 이유를 먼저 명시**하고,
   그 다음에 대안(웹서치 등)으로 얻은 정보는 "링크 원문이 아니라 관련 보도/검색
   결과 기반"이라고 명확히 구분해서 제공한다. 추측을 확인된 사실처럼 섞어 쓰지 않는다.
3. 대안 정보로 답변을 대체할지 여부는 사용자에게 확인받거나, 최소한 사용자가
   구분할 수 있도록 명시적으로 표시한다. 임의로 "확인했다"는 인상을 주지 않는다.
4. 여러 URL 변형(www 유무, .com/.net 등)이나 다른 fetch 경로를 시도해봤는데도
   안 되면, 그 시도 내역을 간단히 알려주고 사용자에게 본문/스크린샷 붙여넣기 등
   대안을 제안한다.
